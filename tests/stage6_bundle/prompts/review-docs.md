Review the generated docs update before any git publication step runs.

Use `:review-focus` to drive the checkpoint. Treat this as the place where a human or a stricter review agent would decide whether the generated output is good enough to publish.
