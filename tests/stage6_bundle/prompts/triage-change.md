Review the incoming `repo-change` signal and summarize the work for the rest of the pipeline.

Use `:change-summary` as the canonical summary of the payload. Call out anything urgent and keep the note short enough that a later execution could reuse it.
