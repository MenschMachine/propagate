Pretend to lint the downstream docs repository before any content updates happen.

If you were adapting this example for a real project, this step could run formatting, link checks, or schema validation before the write step starts.
