This placeholder directory represents the downstream `docs-site` repository used by the stage-6 example config.

Initialize it as a git repository if you want to run the manual `archive-review` or `publish-docs` executions.
