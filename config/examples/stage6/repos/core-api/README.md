This placeholder directory represents the upstream `core-api` repository used by the stage-6 example config.

The example routes `prepare-core-context` here so you can see repository-local context initialization without needing a real codebase.
