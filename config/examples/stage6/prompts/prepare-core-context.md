Pretend to inspect the upstream core repository and write down the implementation details that the docs site needs.

Use `:core-change-note` plus the raw `:signal.*` values to keep the note grounded in the trigger payload.
