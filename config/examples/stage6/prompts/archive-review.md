Archive the review state in the docs repository.

This execution demonstrates `git.commit.message_key`, so the reserved `:archive-commit-message` context value should already exist before this prompt runs.
