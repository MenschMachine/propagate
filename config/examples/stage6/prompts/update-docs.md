Pretend to update the downstream docs repository in response to the upstream change.

Use `:docs-outline` as the task list. The bundled demo agent only writes the rendered prompt to `PROPAGATE_EXAMPLE_OUTPUT.md`, but a real agent would edit docs files here.
