Run signal-driven executions from this directory when you want to demonstrate the invocation working directory.

Executions that omit `repository` use this folder, while repository-routed executions use the paths under `../repos/`.
