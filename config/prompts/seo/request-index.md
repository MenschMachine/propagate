# Confirm Indexing Requests

The `request_indexing.py` script has been run. Check `:indexing-results` in context to see what happened.

Summarize:
- How many URLs were submitted for re-indexing
- Any errors that occurred
- The list of URLs that were submitted

If no URLs were submitted (`:changed-urls` was empty), note that no indexing requests were needed.
