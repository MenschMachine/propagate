# Summarize Changes

Run `git diff` to see what changed. Write a clear, concise summary of the changes suitable for a pull request description.

Save the summary to context:

```
propagate context set :summary "<summary text>"
```
