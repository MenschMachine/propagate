# Generate Branch Name

Read the instructions provided in the context under `:signal.instructions`.

Generate a short, git-safe branch name that describes the work. Use lowercase letters, numbers, and hyphens only. Prefix with `feat/`, `fix/`, or `chore/` as appropriate.

Save the branch name to context:

```
propagate context set :branch-name <branch-name>
```
